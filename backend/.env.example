# Copy to .env and set real credentials. Do not commit .env.
# Prisma uses this URL (MySQL or MariaDB).
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/cleanhub"

# Optional — API listen port (defaults to 3001 in backend code).
# PORT=3001
