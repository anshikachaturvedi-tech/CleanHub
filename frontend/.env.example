# Point the React app at a remote API in production, if not same-origin
# (Leave unset when using Vite dev proxy: npm run dev)
# VITE_API_URL=https://api.example.com
VITE_API_URL=https://backend-production-bc1c.up.railway.app

